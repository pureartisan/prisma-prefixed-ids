# Example environment configuration for testing

# Set database provider for integration tests
# Options: sqlite (default), postgresql, mysql
TEST_DATABASE_PROVIDER=sqlite

# Database connection strings for testing
# IMPORTANT: Use TEST_DATABASE_URL to avoid accidentally affecting production databases

# PostgreSQL (when TEST_DATABASE_PROVIDER=postgresql)
# TEST_DATABASE_URL="postgresql://user:password@localhost:5432/prisma_prefixed_ids_test"

# MySQL (when TEST_DATABASE_PROVIDER=mysql)
# TEST_DATABASE_URL="mysql://user:password@localhost:3306/prisma_prefixed_ids_test"
